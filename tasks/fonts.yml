---
- name: Install fonts packages
  become: true
  kewlfft.aur.aur:
    name: ttf-meslo-nerd-font-powerlevel10k
    use: yay
    state: present
  changed_when: false

- name: Refresh font cache
  command: fc-cache -fv
